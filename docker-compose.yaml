services:
#  webapi:
#    image: webapi
#    build:
#      context: .
#      dockerfile: WebAPI/Dockerfile
  db:
    container_name: reloading-db
    image: postgres:17
    ports: 
      - 5432:5432
    shm_size: 128mb
    environment:
      POSTGRES_DB: reloadingdb
      POSTGRES_USER: reloadinguser
      POSTGRES_PASSWORD: HE110jed
    restart: unless-stopped
    volumes:
      - reloading-data:/var/lib/postgresql/data
volumes:
  reloading-data: